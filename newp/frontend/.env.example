# Point this at your api-gateway. If unreachable, the app falls back
# to bundled mock data automatically (src/mockData.js) so the UI is
# always demoable even if the backend isn't running.
VITE_API_BASE_URL=http://localhost:8080
